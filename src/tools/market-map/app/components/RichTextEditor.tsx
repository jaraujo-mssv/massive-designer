// Moved to @/shared/components/RichTextEditor — Top List and Bento Map use the
// same editor. Re-exported here so this tool's imports keep working.
export { RichTextEditor, applyStyle } from '@/shared/components/RichTextEditor';
